{
  "name": "hellots",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "dev:client": "client/node_modules/.bin/wait-port 3000 && tsx watch client",
    "dev:server": "tsx watch server",
    "dev": "run-p dev:* --print-label",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsx": "^3.12.6"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "npm-run-all": "^4.1.5"
  }
}
